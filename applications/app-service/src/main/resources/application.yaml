##Spring Configuration
server:
  port: 8080
spring:
  application:
    name: MicroserviceAuth
  devtools:
    add-properties: false
  h2:
    console:
      enabled: true
      path: /h2
  profiles:
    include:
adapters:
  r2dbc:
    database: crediya-auth-db
    host: localhost
    port: 5432
    username: postgres
    password: root
    schema: public
